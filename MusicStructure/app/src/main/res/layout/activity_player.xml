<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.example.android.musicstructure.PlayerActivity">

        <Button
            android:id="@+id/library"
            style="@style/CategoryStyle"
            android:background="@color/category_library"
            android:text="Library" />

        <Button
            android:id="@+id/payment"
            style="@style/CategoryStyle"
            android:background="@color/category_payment"
            android:text="Payment" />

        <Button
            android:id="@+id/player"
            style="@style/CategoryStyle"
            android:background="@color/category_player"
            android:text="Player" />

        <Button
            android:id="@+id/store"
            style="@style/CategoryStyle"
            android:background="@color/category_store"
            android:text="Store" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp"
            android:textSize="14sp"
            android:text="This is the page where I would put a player, quite Google-Play-Music-like with cover art bleeding to the edge of the screen, and the controls at the bottom. Again, the MediaPlayer API would be a good start, with listeners for control purposes. I wouldd like to implement shuffle (fisher-yates algorithm could be useful here), plus like and dislike buttons."/>

    </LinearLayout>
</ScrollView>